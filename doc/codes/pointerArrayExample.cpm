func mein(): int {
    int array[5];
    for (int i = 0; i < 5; i = i + 1) {
        array[i] = i;
    }
    int~ ptr = array;
    for (int i = 0; i < 5; i = i + 1) {
        ceaut(array[i]);
        ceaut(~ptr);
        ptr = ptr + 1;
    }

    return 0;
}