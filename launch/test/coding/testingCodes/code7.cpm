estrut coor {
    int x = 0;
    int y = 0;
}

func calculaVector(coor p, coor q) : coor {
    coor res;
    res.x = q.x - p.x;
    res.y = q.y - p.y;
    return res;
}

func mein() : int {
    coor p, q;
    p.x = 1;
    p.y = 3;
    q.x = 3;
    q.y = 3;
    coor vecpq = calculaVector(p, q);
    ceaut(vecpq.x);
    ceaut(vecpq.y);
    return 0;
}

/*
RESULT
2
0

*/