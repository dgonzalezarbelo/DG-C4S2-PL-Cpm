// Switch with a bool expression
difain CONST_TRUE tru;

func mein() :int {
    bull varBool = CONST_TRUE;

    suich (varInt) {
        queis (tru): 
            ceaut(1);
        queis (fols): 
            ceaut(0);
        difolt:
            ceaut(-1000);
    } 

    return 0;
}

/* 
RESULT: 
    1
    0
    -1000

*/
