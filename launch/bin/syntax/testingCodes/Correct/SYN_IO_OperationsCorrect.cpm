//Finished
func simpleFunction(bul result, int num): bul {
    bul aux = tru;
    if (num > 0) {aux = result;}
    return b;
} 

func mein() :int {
    int in;
    bul b = tru;
    int array[2];
    array[0] = 1;
    array[1] = 2;
    
    cein(in);
    int num = 5 + in;
    ceaut(num);
    ceaut(num + num);
    ceaut(4);
    ceaut(array[0]);
    ceaut(simpleFunction(b, in));

    return 0;
}